{
  "name": "examples-langchain",
  "version": "0.0.0",
  "private": true,
  "description": "LangChain (Python) example loading @openpact/skill tools.json at boot",
  "license": "SEE LICENSE IN LICENSE",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "NODE_OPTIONS='--import tsx' brittle 'test/**/*.test.ts'"
  },
  "devDependencies": {
    "@openpact/daemon": "*",
    "@openpact/skill": "*"
  }
}
